import { GradebookManager } from "@/components/GradebookManager";

export const metadata = { title: "Notes | Gabon Éduc+" };

export default function NotesPage() {
  return <GradebookManager module="notes" />;
}
