import { GradebookManager } from "@/components/GradebookManager";

export const metadata = { title: "Bulletins | Gabon Éduc+" };

export default function BulletinsPage() {
  return <GradebookManager module="bulletins" />;
}
